# 用户私有配置(密钥只进 config.json,永不进仓;公开模板是各模块 config.example.json)
config.json

# 运行时生成产物(orchestrator 每轮覆盖,UTF-16 编码)
data.inc
todos.inc
health.json
installed.json
state/
*.log

# 模块运行时渲染的资产(采集器每轮重绘,非源文件)
modules/clock-calendar/assets/

# todo 模块用户数据 + 运行时文件(采集器/交互脚本读写,非源文件;config.json 已由顶部规则覆盖)
modules/todo/todos.json
modules/todo/.clicks

# Python 缓存
__pycache__/
*.pyc
.pytest_cache/

# 施工过程文件(subagent-driven development 报告/进度,不入仓)
.superpowers/
.worktrees/
