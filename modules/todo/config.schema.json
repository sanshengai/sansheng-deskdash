{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "todo 配置契约",
  "description": "零网络零密钥;仅两项部署辅助配置(交互后刷新皮肤用),都可缺省。",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "skin_name": {"type": "string", "description": "部署后的皮肤 config 名(如 DeskdashDemo);交互后 !Refresh 定向刷新。留空=交互脚本走 !RefreshApp 全刷。部署脚本(Task 7)自动填。"},
    "rainmeter_exe": {"type": "string", "description": "Rainmeter.exe 路径(缺省用标准安装路径 C:\\Program Files\\Rainmeter\\Rainmeter.exe);仅交互后刷新用。"}
  }
}
